<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.position">

<!-- 직급 등록 -->
<select id="sequence" resultType="int">
	select position_seq.nextval from dual
</select>
<insert id="add">
	insert into position(
		position_no, position_name, position_block, position_info
	)
	values(
		#{positionNo}, #{positionName}, #{positionBlock}, #{positionInfo}
	)
</insert>

<!-- 직급 정보 조회 -->
<select id="find" resultType="positionDto">
	select * from position where position_no = #{positionNo}
</select>

<!-- 직급 목록 조회 -->
<select id="list" resultType="PositionListVO">
	select * from position order by position_block asc, position_no asc
</select>

<!-- 직급 목록 페이지네이션을 위한 목록 개수 -->
<select id="count" resultType="int">
	select count(*) from position
</select>

<!-- 직급 목록 조회(페이지네이션) -->
<select id="selectList" resultType="PositionListVO">
select * from(
	select rownum rn, TMP.* from
	(
		select * from position 
		<choose>
			<when test="sort == 'positionNo'">
				<choose>
					<when test="direction == 'desc'">
						order by position_no desc
					</when>
					<otherwise>
						order by position_no asc
					</otherwise>
				</choose>
			</when>
			<when test="sort == 'positionName'">
				<choose>
					<when test="direction == 'desc'">
						order by position_name desc, position_no asc
					</when>
					<otherwise>
						order by position_name asc, position_no asc
					</otherwise>
				</choose>
			</when>
			<when test="sort == 'positionOrder'">
				<choose>
					<when test="direction == 'desc'">
						order by position_order desc, position_no asc
					</when>
					<otherwise>
						order by position_order asc, position_no asc
					</otherwise>
				</choose>
			</when>
			<when test="sort == 'positionBlock'">
				<choose>
					<when test="direction == 'desc'">
						order by position_block desc, position_no asc
					</when>
					<otherwise>
						order by position_block asc, position_no asc
					</otherwise>
				</choose>
			</when>
			<otherwise>
				order by position_no asc
			</otherwise>
		</choose>
	)TMP
)
where rn between #{beginRownum} and #{endRownum}
</select>

<!-- 직급 정보 수정 -->
<update id="updateAll">
	update position
	set position_name = #{positionName},
		position_info = #{positionInfo},
		position_order = #{positionOrder},
		position_block = #{positionBlock}
	where position_no = #{positionNo}
</update>

<!-- 직급 목록 조회(검색용) -->
<select id="listSearch" resultType="PositionListSearchVO">
	select * from position where position_block = 'Y' order by position_no asc
</select>













</mapper>