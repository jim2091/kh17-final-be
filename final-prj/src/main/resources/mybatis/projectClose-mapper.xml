<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.projectClose">

	<!-- 프로젝트 종료 등록 -->
	<insert id="add">
		insert into project_close(
			project_no,
			close_summary,
			close_good,
			close_bad,
			close_improvement
		)
		values(
			#{projectNo},
			#{closeSummary},
			#{closeGood},
			#{closeBad},
			#{closeImprovement}
		)
	</insert>
	
	<!-- 프로젝트 종료 삭제 -->
	<delete id="delete">
		delete from project_close
		where project_no = #{projectNo}
	</delete>
</mapper>