<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.taskcomment">

	<!-- 댓글 등록 -->
	<select id="sequence" resultType="int">
		select seq_task_comment_no.nextval from dual
	</select>
	<insert id="add">
		insert into task_comment (
		task_comment_no,
		task_no
		project_name_no,
		task_comment_content,
		task_comment_ctime,
		task_comment_utime
		)
		values(
		#{taskCommentNo},
		#{taskNo},
		#{projectNameNo},
		#{taskCommentContent},
		#{taskCommentCtime},
		#{taskCommentUtime)
		)
	</insert>
	
	<!-- 댓글 조회 -->
	<select id="findComment" resultType="TaskCommentDto">
		select
			tc.task_comment_no  	as taskCommentNo,
			tc.task_no 				as taskNo,
			tc.project_member_no	as projectMemberNo,
			tc.task_comment_content	as taskCommentContent,
			
	</select>

</mapper>