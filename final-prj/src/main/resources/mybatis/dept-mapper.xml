<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.dept">

<!-- 부서 등록 -->
<select id="sequence" resultType="int">
	select dept_seq.nextval from dual
</select>
<insert id="add">
	insert into dept(
		dept_no, dept_name, dept_block, dept_info
	)
	values(
		#{deptNo}, #{deptName}, #{deptBlock}, #{deptInfo}
	)
</insert>

<!-- 부서 정보 조회 -->
<select id="find" resultType="deptDto">
	select * from dept where dept_no = #{deptNo}
</select>

<!-- 부서 목록 조회 -->
<select id="list" resultType="DeptListVO">
	select * from dept order by dept_block asc, dept_no asc
</select>

<!-- 부서 목록 페이지네이션을 위한 목록 개수 -->
<select id="count" resultType="int">
	select count(*) from dept
</select>

<!-- 부서 목록 조회(페이지네이션 -->
<select id="selectList" resultType="DeptListVO">
select * from(
	select rownum rn, TMP.* from
	(
		select * from dept 
		
	<choose>
		<when test="sort == 'deptNo'">
			<choose>
				<when test="direction == 'desc'">
					order by dept_no desc
				</when>
				<otherwise>
					order by dept_no asc
				</otherwise>
			</choose>
		</when>
		<when test="sort == 'deptName'">
			<choose>
				<when test="direction == 'desc'">
					order by dept_name desc, dept_no asc
				</when>
				<otherwise>
					order by dept_name asc, dept_no asc
				</otherwise>
			</choose>
		</when>
		<when test="sort == 'deptBlock'">
			<choose>
				<when test="direction == 'desc'">
					order by dept_block desc, dept_no asc
				</when>
				<otherwise>
					order by dept_block asc, dept_no asc
				</otherwise>
			</choose>
		</when>
		<otherwise>
			order by dept_no asc
		</otherwise>
	</choose>
	)TMP
)
where rn between #{beginRownum} and #{endRownum}
</select>

<!-- 부서 정보 수정  -->

<update id="updateAll">
	update dept
	set dept_name = #{deptName},
		dept_info = #{deptInfo},
		dept_block = #{deptBlock}
	where dept_no = #{deptNo}
</update>

<!-- 부서 목록 조회(검색용) -->
<select id="listSearch" resultType="DeptListSearchVO">
	select * from dept where dept_block = 'Y' order by dept_no asc
</select>











</mapper>