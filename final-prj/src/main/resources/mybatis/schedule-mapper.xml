<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.schedule">

	<!-- 시퀀스 -->
	<select id="sequence" resultType="int">
		select schedule_seq.nextval from dual
	</select>
	
	<!-- 등록 -->
	<insert id="add">
		insert into schedule (
			schedule_no, project_no, schedule_writer_no,
			schedule_title, schedule_content, schedule_start,
			schedule_end, schedule_place
		)
		values (
			#{scheduleNo}, #{projectNo}, #{scheduleWriterNo},
			#{scheduleTitle}, #{scheduleContent}, #{scheduleStart},
			#{scheduleEnd}, #{schedulePlace}
		)
	</insert>
	
	<!-- 수정 -->
	<update id="update">
		update schedule
		set schedule_title = #{scheduleTitle},
			schedule_content = #{scheduleContent},
			schedule_start = #{scheduleStart},
			schedule_end = #{scheduleEnd},
			schedule_place = #{schedulePlace},
			schedule_utime = systimestamp
		where schedule_no = #{scheduleNo}
	</update>
	
	<!-- 삭제 -->
	<delete id="delete">
		delete schedule where schedule_no = #{scheduleNo}
	</delete>
	
	<!-- 상세 조회 -->
	<select id="find" resultType="ScheduleDto">
		select * from schedule
		where schedule_no = #{scheduleNo}
	</select>
	
	<!-- 목록 조회 -->
	<select id="list" resultType="ScheduleEventVO">
	    select
	        schedule_no,
	        schedule_writer_no,
	        schedule_title,
	        schedule_start,
	        schedule_end,
	        schedule_place
	    from schedule
	    where project_no = #{projectNo}
	    order by schedule_start asc
	</select>
</mapper>