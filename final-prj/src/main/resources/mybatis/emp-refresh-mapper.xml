<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mapper.empRefresh">
<!-- 등록/갱신 구문 -->
	<insert id="add">
		insert into emp_refresh(
			emp_no, user_agent, 
			user_address, token_value
		)
		values(
			#{empNo}, #{userAgent}, 
			#{userAddress}, #{tokenValue}
		)
	</insert>
	<update id="change">
		update emp_refresh
		set token_value = #{tokenValue}
		where 	emp_no = #{empNo} 
				and 
				user_agent = #{userAgent}
				and
				user_address = #{userAddress}
	</update>
	<!-- 상세 조회구문 -->
	<select id="find" resultType="EmpRefreshDto">
		select * from emp_refresh 
		where 	emp_no = #{empNo} 
				and 
				user_agent = #{userAgent}
				and
				user_address = #{userAddress}
	</select>
	<!-- 삭제 -->
	<delete id="delete">
		delete emp_refresh 
		where 	emp_no = #{empNo} 
				and 
				user_agent = #{userAgent}
				and
				user_address = #{userAddress}
	</delete>

</mapper>